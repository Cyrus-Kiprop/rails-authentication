<%# <p id="notice"><%= notice %></p>


<section>
  <div class="container">
    <div class="row">
      <div class="col-3">
        <%= render 'naughty_feed' %>
      </div>

      <div class="col-6 p-0">
        <% if !user_signed_in? %>
        <h1 class="text-center latest-posts">Trending Posts</h1>
        <% end %>
        <%= render 'member_feed'  %>
      </div>

      <div class="col-3">
        <%= render 'trending_feed' %>
      </div>
    </div>
  </div>
</section>

<!-- <table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% Post.all.each do |post| %>
      <tr>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<br>
